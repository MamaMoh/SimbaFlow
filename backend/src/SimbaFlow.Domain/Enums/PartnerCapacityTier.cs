namespace SimbaFlow.Domain.Enums;

/// <summary>
/// Art. 40 — max Ethiopian agencies that may link to one foreign partner.
/// Low=2, Medium=4, High=8.
/// </summary>
public enum PartnerCapacityTier
{
    Low = 0,
    Medium = 1,
    High = 2
}
